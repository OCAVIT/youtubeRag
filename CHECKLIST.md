# ✅ Checklist перед деплоем на Railway

Используйте этот чеклист для проверки готовности проекта к деплою.

## 📋 Предварительная проверка

- [ ] Все файлы проекта созданы
- [ ] .gitkeep файлы на месте в пустых директориях
- [ ] .gitignore корректно настроен
- [ ] .dockerignore корректно настроен
- [ ] README.md содержит полную документацию
- [ ] requirements.txt содержит все зависимости

## 🐳 Локальное тестирование (Docker)

- [ ] Проект собирается без ошибок:
  ```bash
  docker build -t youtuberag .
  ```

- [ ] Контейнер запускается:
  ```bash
  docker run -p 5055:5055 youtuberag
  ```

- [ ] Health check отвечает:
  ```bash
  curl http://localhost:5055/health
  ```

- [ ] Все эндпоинты работают:
  ```bash
  ./test_local.sh
  ```

## 🐳 Локальное тестирование (Docker Compose)

- [ ] Docker Compose запускается:
  ```bash
  docker-compose up -d --build
  ```

- [ ] Логи выводятся корректно:
  ```bash
  docker-compose logs -f
  ```

- [ ] Контейнер работает стабильно:
  ```bash
  docker-compose ps
  ```

- [ ] Volumes примонтированы:
  ```bash
  ls -la output/ transcripts/
  ```

- [ ] Остановка работает корректно:
  ```bash
  docker-compose down
  ```

## 🔧 Код и конфигурация

- [ ] server.py использует переменную окружения PORT
- [ ] Gunicorn настроен в Dockerfile
- [ ] Healthcheck добавлен в Dockerfile
- [ ] CORS включён для всех origins
- [ ] Логирование выводится в stdout/stderr
- [ ] Обработка ошибок возвращает JSON
- [ ] Временные файлы автоматически удаляются

## 🧪 Функциональное тестирование

### Эндпоинт /health
- [ ] Возвращает `{"status": "ok"}`
- [ ] HTTP статус 200

### Эндпоинт /transcribe
- [ ] Работает с video_url
- [ ] Работает с file_path
- [ ] Возвращает text, segments, language
- [ ] Обрабатывает ошибки корректно

### Эндпоинт /extract-frames
- [ ] Скачивает видео
- [ ] Извлекает кадры с заданным интервалом
- [ ] Возвращает task_id и frames_count
- [ ] Кадры сохраняются в output/frames/

### Эндпоинт /process-video
- [ ] Создаёт фоновую задачу
- [ ] Возвращает task_id и status
- [ ] Поддерживает операции "transcribe" и "extract_frames"

### Эндпоинт /status/<task_id>
- [ ] Возвращает статус задачи
- [ ] Показывает результат при завершении
- [ ] Показывает ошибку при сбое

### Эндпоинт /exec
- [ ] Выполняет Python код
- [ ] Возвращает stdout и stderr
- [ ] Таймаут работает (30 секунд)
- [ ] Обрабатывает ошибки

## 📦 Git репозиторий

- [ ] Git инициализирован:
  ```bash
  git init
  ```

- [ ] Все файлы добавлены:
  ```bash
  git add .
  ```

- [ ] Первый коммит создан:
  ```bash
  git commit -m "Initial commit: youtubeRag ready for Railway"
  ```

- [ ] Remote добавлен:
  ```bash
  git remote add origin <repo-url>
  ```

- [ ] Код запушен:
  ```bash
  git push -u origin main
  ```

## 🚂 Railway деплой

- [ ] Аккаунт Railway создан
- [ ] GitHub аккаунт подключен к Railway
- [ ] Проект создан в Railway
- [ ] Репозиторий выбран
- [ ] Деплой запущен автоматически
- [ ] Билд прошёл успешно
- [ ] Сервис запустился
- [ ] Public URL получен

## 🌐 Railway тестирование

- [ ] Health check работает:
  ```bash
  curl https://your-url.up.railway.app/health
  ```

- [ ] Транскрипция работает:
  ```bash
  curl -X POST https://your-url.up.railway.app/transcribe \
    -H "Content-Type: application/json" \
    -d '{"video_url": "https://www.youtube.com/watch?v=jNQXAC9IVRw"}'
  ```

- [ ] Логи доступны в Railway Dashboard

- [ ] Метрики отображаются (CPU, Memory, Network)

## 🔗 Интеграция с n8n

- [ ] n8n установлен и настроен
- [ ] HTTP Request нода создана
- [ ] URL Railway сервиса добавлен
- [ ] Тестовый запрос успешен
- [ ] Результаты обрабатываются корректно

## 📊 Мониторинг

- [ ] Railway логи проверены
- [ ] Нет критических ошибок
- [ ] Memory usage в норме (<1GB)
- [ ] CPU usage в норме
- [ ] Сервис автоматически перезапускается при сбое

## 🔐 Безопасность

- [ ] .env файлы не закоммичены
- [ ] Секреты не хранятся в коде
- [ ] /exec эндпоинт защищён или отключен (опционально)
- [ ] CORS настроен правильно
- [ ] Healthcheck не раскрывает чувствительную информацию

## 📝 Документация

- [ ] README.md актуален
- [ ] DEPLOY.md содержит инструкции
- [ ] TEST_COMMANDS.md содержит примеры
- [ ] n8n_examples.json содержит примеры для n8n
- [ ] Все комментарии в коде на русском или английском

## 🎯 Production готовность

- [ ] Gunicorn используется вместо Flask dev server
- [ ] Workers настроены (2 workers, 4 threads)
- [ ] Timeout установлен (300 секунд)
- [ ] Graceful shutdown реализован
- [ ] Автоочистка временных файлов работает
- [ ] Ошибки логируются корректно

## 🚀 Финальная проверка

- [ ] Проект работает на Railway
- [ ] URL доступен из браузера
- [ ] API отвечает на запросы
- [ ] n8n успешно интегрирован
- [ ] Документация полная и актуальная
- [ ] Все тесты проходят

---

## ✅ Готово к продакшену!

Если все пункты отмечены, ваш youtubeRag сервер готов к использованию!

**Следующие шаги:**
1. Поделитесь URL с командой
2. Настройте мониторинг (опционально)
3. Добавьте кастомный домен (опционально)
4. Настройте CI/CD для автоматических деплоев (опционально)

**Полезные команды:**

```bash
# Просмотр логов
# Railway: Dashboard -> Deployments -> Logs

# Локальные логи
docker-compose logs -f

# Перезапуск сервиса на Railway
# Railway: Dashboard -> Settings -> Restart

# Обновление кода
git add .
git commit -m "Update: ..."
git push origin main
# Railway автоматически задеплоит новую версию
```
