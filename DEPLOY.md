# 🚀 Инструкция по деплою на Railway

Пошаговое руководство по деплою youtubeRag на Railway.

## Шаг 1: Подготовка репозитория

```bash
# Перейдите в директорию проекта
cd youtubeRag

# Инициализируйте Git (если ещё не инициализирован)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: youtubeRag ready for Railway"

# Создайте репозиторий на GitHub/GitLab
# Затем добавьте remote и запушьте
git remote add origin <your-repo-url>
git branch -M main
git push -u origin main
```

## Шаг 2: Деплой на Railway

### Вариант А: Через веб-интерфейс (рекомендуется)

1. **Перейдите на [railway.app](https://railway.app)**

2. **Войдите или зарегистрируйтесь**
   - Можно через GitHub аккаунт

3. **Создайте новый проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Авторизуйте Railway для доступа к GitHub
   - Выберите репозиторий `youtubeRag`

4. **Railway автоматически**:
   - Обнаружит Dockerfile
   - Начнёт билд
   - Создаст публичный URL

5. **Подождите завершения деплоя** (3-5 минут)
   - Следите за логами в реальном времени
   - Должно появиться: "Deployment successful"

6. **Получите URL**
   - Нажмите на "Settings"
   - Найдите "Domains"
   - Скопируйте URL (например: `https://youtuberag-production.up.railway.app`)

### Вариант Б: Через Railway CLI

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите
railway login

# Создайте проект
railway init

# Деплой
railway up

# Откройте в браузере
railway open
```

## Шаг 3: Проверка работы

```bash
# Замените на ваш URL
RAILWAY_URL="https://youtuberag-production.up.railway.app"

# Проверка health check
curl $RAILWAY_URL/health

# Ожидаемый ответ:
# {"status":"ok","timestamp":"2024-01-15T12:00:00"}
```

## Шаг 4: Тестирование API

```bash
# Тест транскрипции (займёт несколько минут)
curl -X POST $RAILWAY_URL/transcribe \
  -H "Content-Type: application/json" \
  -d '{"video_url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ"}'

# Тест извлечения кадров
curl -X POST $RAILWAY_URL/extract-frames \
  -H "Content-Type: application/json" \
  -d '{"video_url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ", "interval_sec": 10}'
```

## Шаг 5: Настройка n8n

1. Откройте n8n
2. Создайте новый Workflow
3. Добавьте HTTP Request ноду
4. Настройте:
   - **Method**: POST
   - **URL**: `https://youtuberag-production.up.railway.app/transcribe`
   - **Body**: `{"video_url": "{{ $json.url }}"}`

## Мониторинг и логи

### Просмотр логов

1. Откройте проект на Railway
2. Перейдите в "Deployments"
3. Нажмите на текущий деплой
4. Вкладка "Logs"

### Метрики

- Railway автоматически показывает:
  - CPU usage
  - Memory usage
  - Network traffic
  - Deployment status

## Обновление кода

```bash
# Внесите изменения в код
# Закоммитьте и запуште
git add .
git commit -m "Update: описание изменений"
git push origin main

# Railway автоматически задеплоит новую версию
```

## Настройка переменных окружения (опционально)

На Railway в разделе "Variables":

```bash
# Добавить переменную (если нужно)
WHISPER_MODEL=base
MAX_VIDEO_LENGTH=3600
```

Затем обновите [server.py](server.py) для использования этих переменных.

## Troubleshooting

### Проблема: Build fails

**Решение**:
1. Проверьте логи билда
2. Убедитесь что все файлы закоммичены
3. Проверьте Dockerfile на ошибки

```bash
# Проверка локально
docker build -t youtuberag .
docker run -p 5055:5055 youtuberag
```

### Проблема: Deployment successful, но сервер не отвечает

**Решение**:
1. Проверьте логи деплоймента
2. Убедитесь что переменная PORT используется правильно
3. Проверьте healthcheck:

```bash
curl https://your-url.up.railway.app/health
```

### Проблема: Memory exceeded

**Решение**:
1. Увеличьте plan на Railway (если нужно)
2. Оптимизируйте код для использования меньше памяти
3. Whisper модель "base" использует ~1GB RAM

### Проблема: Timeout errors

**Решение**:
1. Используйте `/process-video` для длинных видео (фоновая обработка)
2. Увеличьте timeout в Gunicorn (уже 300s)
3. Разбивайте большие видео на части

## Стоимость

Railway предоставляет:
- **Free tier**: $5 бесплатных кредитов в месяц
- **Hobby plan**: $5/месяц за проект
- **Pro plan**: от $20/месяц

Оценка для youtubeRag:
- Простой (несколько запросов в день): Free tier достаточно
- Средняя нагрузка: ~$5-10/месяц
- Высокая нагрузка: ~$20-50/месяц

## Оптимизация для production

### 1. Добавьте Redis для кеширования

```bash
# На Railway добавьте Redis service
# Обновите код для кеширования результатов транскрипции
```

### 2. Настройте мониторинг

```bash
# Добавьте сервис мониторинга (Sentry, LogRocket)
pip install sentry-sdk
```

### 3. Используйте CDN для кадров

```bash
# Загружайте кадры в S3/Cloudflare R2
# Возвращайте ссылки вместо локальных путей
```

## Полезные ссылки

- [Railway Docs](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [Faster Whisper Docs](https://github.com/guillaumekln/faster-whisper)
- [n8n Docs](https://docs.n8n.io)

## Готово! 🎉

Ваш youtubeRag сервер работает на Railway и готов принимать запросы от n8n!

**URL вашего сервера**: `https://youtuberag-production.up.railway.app`
