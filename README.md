# youtubeRag - HTTP-сервер для обработки видео

Production-ready Python HTTP-сервер для транскрипции видео, извлечения кадров и выполнения Python кода. Полностью готов к деплою на Railway и работе с n8n.

## 🚀 Возможности

- **Транскрипция видео** - автоматическое преобразование речи в текст с помощью faster-whisper
- **Извлечение кадров** - автоматическое извлечение кадров из видео с заданным интервалом
- **Фоновая обработка** - асинхронная обработка видео с отслеживанием статуса
- **Выполнение Python кода** - безопасное выполнение произвольного Python кода
- **Автоочистка** - автоматическое удаление временных файлов
- **CORS** - поддержка кросс-доменных запросов для интеграции с n8n

## 📦 Быстрый старт

### Деплой на Railway (рекомендуется)

1. **Создайте репозиторий на GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

2. **Деплой на Railway**
   - Перейдите на [railway.app](https://railway.app)
   - Нажмите "New Project" → "Deploy from GitHub repo"
   - Выберите ваш репозиторий
   - Railway автоматически обнаружит Dockerfile и начнёт деплой
   - После деплоя скопируйте URL вашего сервиса (например, `https://youtuberag-production.up.railway.app`)

3. **Проверьте работу**
   ```bash
   curl https://your-railway-url.up.railway.app/health
   ```

### Локальная разработка через Docker Compose

```bash
# Клонирование репозитория
git clone <repo-url>
cd youtubeRag

# Запуск
docker-compose up -d --build

# Проверка работы
curl http://localhost:5055/health

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### Локальная разработка без Docker

```bash
# Установка зависимостей
pip install -r requirements.txt

# Установка ffmpeg (Linux/Mac)
# Ubuntu/Debian: sudo apt-get install ffmpeg
# Mac: brew install ffmpeg
# Windows: скачайте с ffmpeg.org

# Запуск сервера
python server.py
```

## 📡 API Эндпоинты

### 1. Health Check
**GET** `/health`

Проверка работоспособности сервера.

**Пример:**
```bash
curl http://localhost:5055/health
```

**Ответ:**
```json
{
  "status": "ok",
  "timestamp": "2024-01-15T12:00:00"
}
```

---

### 2. Транскрипция видео
**POST** `/transcribe`

Транскрибирует видео в текст с использованием faster-whisper.

**Body:**
```json
{
  "video_url": "https://www.youtube.com/watch?v=..."
}
```
или
```json
{
  "file_path": "/app/input/video.mp4"
}
```

**Пример:**
```bash
curl -X POST http://localhost:5055/transcribe \
  -H "Content-Type: application/json" \
  -d '{"video_url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ"}'
```

**Ответ:**
```json
{
  "text": "Полный текст транскрипции...",
  "segments": [
    {
      "start": 0.0,
      "end": 2.5,
      "text": "Привет, это тестовое видео"
    }
  ],
  "language": "ru"
}
```

---

### 3. Извлечение кадров
**POST** `/extract-frames`

Извлекает кадры из видео с заданным интервалом.

**Body:**
```json
{
  "video_url": "https://www.youtube.com/watch?v=...",
  "interval_sec": 5
}
```

**Пример:**
```bash
curl -X POST http://localhost:5055/extract-frames \
  -H "Content-Type: application/json" \
  -d '{"video_url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ", "interval_sec": 5}'
```

**Ответ:**
```json
{
  "task_id": "uuid-here",
  "frames_count": 120,
  "frames_dir": "/app/output/frames/uuid-here",
  "interval_sec": 5.0,
  "video_duration": 600.5
}
```

---

### 4. Фоновая обработка видео
**POST** `/process-video`

Запускает обработку видео в фоновом режиме.

**Body:**
```json
{
  "video_url": "https://www.youtube.com/watch?v=...",
  "operations": ["transcribe", "extract_frames"]
}
```

**Пример:**
```bash
curl -X POST http://localhost:5055/process-video \
  -H "Content-Type: application/json" \
  -d '{"video_url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ", "operations": ["transcribe"]}'
```

**Ответ:**
```json
{
  "task_id": "uuid-here",
  "status": "processing"
}
```

---

### 5. Проверка статуса задачи
**GET** `/status/<task_id>`

Получение статуса фоновой задачи.

**Пример:**
```bash
curl http://localhost:5055/status/uuid-here
```

**Ответ (в процессе):**
```json
{
  "task_id": "uuid-here",
  "status": "processing",
  "video_url": "https://...",
  "operations": ["transcribe"],
  "created_at": "2024-01-15T12:00:00"
}
```

**Ответ (завершено):**
```json
{
  "task_id": "uuid-here",
  "status": "completed",
  "result": {
    "transcription": {
      "text": "...",
      "segments": [...],
      "language": "ru"
    }
  },
  "completed_at": "2024-01-15T12:05:00"
}
```

---

### 6. Выполнение Python кода
**POST** `/exec`

Выполняет произвольный Python код (таймаут 30 секунд).

**Body:**
```json
{
  "code": "print(1 + 1)"
}
```

**Пример:**
```bash
curl -X POST http://localhost:5055/exec \
  -H "Content-Type: application/json" \
  -d '{"code": "import sys\nprint(sys.version)"}'
```

**Ответ:**
```json
{
  "success": true,
  "stdout": "3.11.0\n",
  "stderr": "",
  "error": null
}
```

## 🔗 Интеграция с n8n

### Пример HTTP Request ноды

1. **Создайте HTTP Request ноду** в n8n
2. **Настройте параметры:**

**Для транскрипции:**
- **Method:** POST
- **URL:** `https://your-railway-url.up.railway.app/transcribe`
- **Authentication:** None
- **Send Body:** Yes
- **Body Content Type:** JSON
- **Specify Body:** Using JSON
- **JSON:**
  ```json
  {
    "video_url": "{{ $json.video_url }}"
  }
  ```

**Для фоновой обработки:**
- **Method:** POST
- **URL:** `https://your-railway-url.up.railway.app/process-video`
- **JSON:**
  ```json
  {
    "video_url": "{{ $json.video_url }}",
    "operations": ["transcribe", "extract_frames"]
  }
  ```

**Для проверки статуса:**
- **Method:** GET
- **URL:** `https://your-railway-url.up.railway.app/status/{{ $json.task_id }}`

## 📂 Структура проекта

```
youtubeRag/
├── Dockerfile              # Docker образ для деплоя
├── docker-compose.yml      # Локальная разработка
├── requirements.txt        # Python зависимости
├── server.py              # Основной Flask-сервер
├── .gitignore             # Игнорируемые файлы
├── .dockerignore          # Файлы для исключения из образа
├── README.md              # Документация
├── input/                 # Входные файлы (локальные)
│   └── .gitkeep
├── output/                # Результаты обработки
│   └── .gitkeep
├── temp/                  # Временные файлы
│   └── .gitkeep
└── transcripts/           # Сохранённые транскрипции
    └── .gitkeep
```

## 🔧 Конфигурация

### Переменные окружения

- `PORT` - порт сервера (по умолчанию 5055, Railway устанавливает автоматически)
- `PYTHONUNBUFFERED=1` - отключение буферизации логов

### Railway специфичные настройки

Проект полностью адаптирован под Railway:
- ✅ Автоматическое определение порта из переменной `PORT`
- ✅ Gunicorn для production с оптимальными настройками
- ✅ Healthcheck для мониторинга
- ✅ Логирование в stdout/stderr
- ✅ Graceful shutdown

## 📊 Логи

### Локально (Docker Compose)
```bash
docker-compose logs -f
```

### Railway
- Откройте ваш проект на Railway
- Перейдите в раздел "Deployments"
- Нажмите на текущий деплой
- Вкладка "Logs"

## 🛠️ Troubleshooting

### Проблема: Сервер не запускается
**Решение:** Проверьте логи
```bash
docker-compose logs youtuberag
```

### Проблема: Ошибка при скачивании видео
**Решение:** Убедитесь что URL корректный и видео доступно. Попробуйте другой URL.

### Проблема: Медленная транскрипция
**Решение:** Это нормально для CPU. Для ускорения используйте GPU или модель меньшего размера (tiny, base).

### Проблема: Railway деплой падает
**Решение:**
1. Проверьте логи на Railway
2. Убедитесь что Dockerfile корректный
3. Проверьте что все зависимости указаны в requirements.txt

## 📝 Примечания

- **Автоочистка:** Временные файлы старше 1 часа удаляются автоматически
- **Безопасность:** Эндпоинт `/exec` следует защитить или отключить в продакшене
- **Модель Whisper:** По умолчанию используется модель `base` на CPU. Для GPU измените в коде.
- **Таймауты:** Gunicorn настроен на таймаут 300 секунд (5 минут) для длинных видео

## 🚀 Production Checklist

Перед деплоем на Railway:
- ✅ Все файлы .gitkeep созданы
- ✅ .gitignore настроен правильно
- ✅ Эндпоинт /health отвечает
- ✅ Протестированы основные эндпоинты
- ✅ Логи выводятся корректно
- ✅ Переменная PORT используется из env

## 📄 Лицензия

MIT

## 🤝 Поддержка

Если возникли проблемы - проверьте логи и раздел Troubleshooting в этом README.
